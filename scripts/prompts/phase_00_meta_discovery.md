# Phase 00: Meta-Category Discovery

## Context

Domain context: {domain_context}
Target concept: **{target}**
Meta guidance (optional): {meta_guidance}

## Task

Look at the samples and answer: **What types of content exist here?**

- The target ({target}) must appear as exactly one meta-category
- Identify other content types (e.g., procedures, outcomes, background)
- Include **other** as a catch-all meta-category
- Keep it simple: 3-6 meta-categories maximum

## Rules

- Meta-categories must be **non-overlapping** (each sample fits ONE meta)
- Use simple names one word or snake cases max two words
- Avoid compound categories (no "X_and_Y" or "X_or_Y")
- If meta_guidance is "none", ignore it

## Samples

{samples}
